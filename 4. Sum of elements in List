# WWC Day-4 Python Coding challenge
# Write a program to find the sum of all elements in a list.

def sumList(l):

    total=0
    for i in range(len(l)):
            total+=l[i]
    return total

l=[5,6,8,10,14]
print("Sum of elements in {} is {} ".format(l, sumList(l)))
