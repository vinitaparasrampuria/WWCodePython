# WWC Day-8 Python Coding challenge
# Write a function that accepts a string and calculates the number of uppercase and lowercase letters in it.

def upperLower():
    str=input("Enter a string:")
    upper,lower=0,0
    for s in str:
        if s.isupper():
            upper+=1
        elif s.islower():
            lower+=1
    return upper, lower

upper, lower=upperLower()
print("Number of Upper case characters:", upper)
print("Number of Lower case characters:", lower)


