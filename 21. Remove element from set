# WWC Day-21 Python Coding challenge
# Create a program to remove a specific element from a set.

def removeElement(my_set, element_to_remove):
    try:
        my_set.remove(element_to_remove)
        print(f"Element '{element_to_remove}' removed successfully.")
    except KeyError:
        print(f"Element '{element_to_remove}' not found in the set.")



my_set = {1, 2, 3, 4, 5}

element_to_remove = 3
removeElement(my_set, element_to_remove)

print("Updated Set:", my_set)
